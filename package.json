{
  "name": "latticeag",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "description": "The LatticeAG stack as one command. Every product event, one schema.",
  "license": "MIT",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r exec tsc --noEmit",
    "codegen": "pnpm --filter @latticeag/events codegen"
  }
}
